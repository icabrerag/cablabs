"use client";
import { useState } from"react";import{useRouter}from"next/navigation";import{useReducedMotion}from"framer-motion";import Reveal from"@/components/Reveal";import{useLanguage}from"@/components/providers/LanguageProvider";
export function AlterEgo(){const{t}=useLanguage();const router=useRouter();const reduced=useReducedMotion();const[leaving,setLeaving]=useState(false);function enter(){setLeaving(true);if(reduced){router.push("/flow");return}window.setTimeout(()=>router.push("/flow"),450)}return <section className={`alter-section ${leaving?"is-leaving":""}`} aria-labelledby="alter-title"><div className="alter-noise" aria-hidden="true"/><Reveal className="alter-inner"><p className="section-kicker"><span>05</span>{t.alter.eyebrow}</p><div className="alter-symbol" aria-hidden="true"><span>IC</span><i>/</i><b>FLOW</b></div><h2 id="alter-title">{t.alter.title}</h2><p>{t.alter.copy}<br/><span>{t.alter.subcopy}</span></p><button type="button" className="secret-button" onClick={enter} disabled={leaving}><i aria-hidden="true">◉</i><span>{leaving?t.alter.loading:t.alter.cta}</span><b aria-hidden="true">↗</b></button></Reveal></section>}
