"use client";
import{FormEvent,useState}from"react";import{useLanguage}from"@/components/providers/LanguageProvider";import{professionalSocials}from"@/data/socials";
type FormStatus="idle"|"loading"|"success"|"error"|"unconfigured";
export function Contact(){const{t}=useLanguage();const[status,setStatus]=useState<FormStatus>("idle");const accessKey=process.env.NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY;async function handleSubmit(event:FormEvent<HTMLFormElement>){event.preventDefault();if(!accessKey){setStatus("unconfigured");return}setStatus("loading");const form=event.currentTarget;const data=new FormData(form);data.append("access_key",accessKey);data.append("subject","Nuevo mensaje desde el portfolio");data.append("from_name","Portfolio Ignacio Cabrera");try{const response=await fetch("https://api.web3forms.com/submit",{method:"POST",body:data});const result=await response.json() as{success?:boolean};if(!response.ok||!result.success)throw new Error();form.reset();setStatus("success")}catch{setStatus("error")}}return <section id="contact" className="contact-section"><div className="section-shell contact-shell"><div className="contact-copy"><p className="section-kicker"><span>06</span>{t.contact.eyebrow}</p><h2>{t.contact.line1}<br/><span>{t.contact.line2}</span></h2><p>{t.contact.copy}</p><div className="social-row"><a href={professionalSocials.github} target="_blank" rel="noopener noreferrer">GitHub <span>↗</span></a><a href={professionalSocials.linkedin} target="_blank" rel="noopener noreferrer">in&nbsp; LinkedIn <span>↗</span></a></div></div><form className="contact-form" onSubmit={handleSubmit} aria-label={t.contact.form}><input type="checkbox" name="botcheck" className="honeypot" tabIndex={-1} autoComplete="off"/><label>01 / {t.contact.name}<input name="name" type="text" required minLength={2} autoComplete="name" placeholder={t.contact.namePlaceholder}/></label><label>02 / {t.contact.email}<input name="email" type="email" required autoComplete="email" placeholder="tu@email.com"/></label><label>03 / {t.contact.message}<textarea name="message" required minLength={10} rows={4} placeholder={t.contact.messagePlaceholder}/></label><button className="button button-primary submit-button" type="submit" disabled={status==="loading"}>{status==="loading"?t.contact.sending:t.contact.send}<span aria-hidden="true">↗</span></button><div className="form-status" aria-live="polite">{status==="success"&&<p className="success">{t.contact.success}</p>}{status==="error"&&<p className="error">{t.contact.error}</p>}{status==="unconfigured"&&<p className="error">{t.contact.unconfigured}</p>}</div></form></div></section>}
