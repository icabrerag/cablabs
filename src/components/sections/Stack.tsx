"use client";
import Reveal from"@/components/Reveal";import{useLanguage}from"@/components/providers/LanguageProvider";import{SectionHeading}from"@/components/ui/SectionHeading";import{explored,stackItems}from"@/data/portfolio";
export function Stack(){const{t}=useLanguage();return <section id="stack" className="stack-section section-pad"><div className="section-shell"><Reveal><SectionHeading index="03" eyebrow={t.stack.eyebrow} title={t.stack.title} description={t.stack.description}/></Reveal><div className="stack-grid">{t.stack.groups.map((group,index)=><Reveal key={group.label} delay={index*.06} className="stack-card"><div className="stack-card-head"><span>0{index+1}</span><small>{group.note}</small></div><h3>{group.label}</h3><ul>{stackItems[index].map(item=><li key={item}>{item}<i/></li>)}</ul></Reveal>)}</div><Reveal className="explored-row"><span>{t.stack.explored}</span><div>{explored.map(item=><em key={item}>{item}</em>)}</div></Reveal></div></section>}
