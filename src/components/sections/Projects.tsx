"use client";
import Reveal from"@/components/Reveal";import{useLanguage}from"@/components/providers/LanguageProvider";import{ProjectVisual}from"@/components/ui/ProjectVisual";import{SectionHeading}from"@/components/ui/SectionHeading";import{projects}from"@/data/portfolio";
export function Projects(){const{t}=useLanguage();return <section id="work" className="section-shell section-pad"><Reveal><SectionHeading index="02" eyebrow={t.work.eyebrow} title={t.work.title} description={t.work.description}/></Reveal><div className="projects-grid">{projects.map((project,index)=>{const copy=t.work.cards[project.id];return <Reveal key={project.id} delay={(index%2)*.08} className={`project-card ${index<2?"featured":""} ${index===projects.length-1?"project-last":""}`}><article><ProjectVisual type={project.visual}/><div className="project-content"><div className="project-meta"><span>{copy.eyebrow}</span><span>0{index+1}</span></div><h3>{project.title}</h3><p className="project-description">{copy.description}</p><p className="project-problem">{copy.problem}</p><div className="tag-row">{project.stack.map(item=><span key={item}>{item}</span>)}</div><div className="project-foot">{project.href?<a href={project.href} target="_blank" rel="noopener noreferrer">{t.work.visit}<span aria-hidden="true">↗</span></a>:<span>{project.confidential?t.work.confidential:t.work.independent}</span>}</div></div></article></Reveal>})}</div></section>}
